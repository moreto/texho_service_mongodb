info:
  name: local
  type: http
  seq: 1

http:
  method: GET
  url: "{{HOST}}/v1/organizacao-local"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
