info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{HOST}}/v1/login"
  body:
    type: json
    data: |-
      {
        "email": "mmoreto@gmail.com",
        "senha": "senha123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
